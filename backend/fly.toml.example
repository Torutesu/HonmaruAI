# Fly.io config — copy to fly.toml, set app name, then:
#   fly launch --no-deploy   (once, from backend/)
#   fly volumes create honmaru_data --size 1
#   fly secrets set OPENROUTER_API_KEY=... GITHUB_CLIENT_ID=... GITHUB_CLIENT_SECRET=...
#   fly deploy
app = "honmaru-backend"
primary_region = "nrt"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "8080"
  DATABASE_PATH = "/data/honmaru.db"
  # Production: keep dev login OFF and sign in with GitHub OAuth
  # (set GITHUB_CLIENT_ID/SECRET secrets and GITHUB_REDIRECT_URI to
  # https://<app>.fly.dev/auth/github/callback). Flip to "1" only for demos.
  AUTH_DEV_MODE = "0"
  SLA_SWEEP_SECONDS = "60"

[[mounts]]
  source = "honmaru_data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false   # keep WebSocket + SLA sweeper alive
  min_machines_running = 1
