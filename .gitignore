.DS_Store
build/
DerivedData/
server/node_modules/
server/.env
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside
*.xcscmblueprint

# App Store Connect. The glob also catches .asc.env.bak and similar copies,
# which hold the same credentials as the original.
.asc.env*
*.p8
*.xcarchive
ExportOptions.plist

# Machine-specific relay address, written by scripts/device.sh
Config/Local.xcconfig

# Uploaded clips: runtime data, not source.
server/data/

# Local dev scaffolding (not for production)
worker/.wrangler/
worker/seed-dev.sql
worker/test-join.mjs
