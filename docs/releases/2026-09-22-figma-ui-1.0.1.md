# Honmaru AI 1.0.1 — native UI integration

Base: production main `c90dfc6`. Integration branch: `codex/figma-ui-release-1-0-1`.

## Scope

- Bring the approved Figma-aligned native home, composer, detail, history, profile and explicit local demo onto production main.
- Keep production authentication/session restoration, APNs, StoreKit/RevenueCat configuration, on-device-only transcription, context synchronization, opaque team directory, role-aware team management and connected tools.
- Add backwards-compatible `member:<ref>` recipients. The relay resolves references within the authenticated workspace, still stamps the sender, and returns matching delivery evidence. The directory does not expose login/email identifiers.
- Keep legacy WebSocket snapshots, context events, presence and nudge support. Preserve main's account isolation, recipient-bucket cleanup and late GitHub response guards.
- Preserve existing localization entries and language options; merge the new Japanese/English UI strings.

## Verification recorded so far

- Debug simulator application build: PASS.
- Worker full suite: 73 files / 409 tests PASS, including seven new native-reference routing/security regressions.
- Native unit suite with live login enabled: 95 tests PASS, no skips. Disposable production-account login, `/me` restore, directory access, WebSocket join, self-send and reconnect persistence passed; account cleanup passed.
- Actual UI test: welcome/email password form, local demo, typed request, recipient selection, send, History and Terms/Privacy links PASS.
- Release configuration/privacy/AI consent static gate: PASS.
- Inspected screenshots from actual compiled app, not a screenshot-only fixture.

## Release state / remaining qualification

- App Store Connect 1.0.1 draft already holds the 16 approved banner-v3 screenshots (8 Japanese / 8 English); release mode is manual.
- The compatibility change is not deployed merely by this document or by local tests. Verify deployment and two-account opaque-reference delivery before submission.
- Signed 1.0.1 archive, Release launch smoke test, live UI login/relaunch test, final ASC validation and submission still require their own evidence.
- Connected physical devices were unavailable during this integration run. Earlier physical-device testing of 1.0/build 42 does not certify this new binary. Device microphone, permission denial, purchase/restore and APNs checks remain distinct from simulator QA.
- Apple approval and final manual release are separate from uploading/submitting a build. Do not describe a draft as released.

The original working tree and the approved banner assets were not overwritten.
